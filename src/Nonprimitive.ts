// aray , object

let bazarList = [ "rice", "lentil", "oil", "salt", "sugar" ]

bazarList.push("tea")

let mixedArray = [ "Muntasir", 12345, true ]

mixedArray.push("John")

// ts - tupples

let coordinates: [number, number] = [ 10, 20 ]

let couple: [string, string] = ["Husband", "Wife"]

let nameAndRoll : [string, number] = ["Muntasir", 12345]

let destination : [string, string, number] = ["Dhaka", "Chittagong", 200]

// ts-refference type


const user : {
    firstName: string,
    lastName: string,

} = {
    firstName: "Muntasir",
    lastName: "Mahmud"
}
  

